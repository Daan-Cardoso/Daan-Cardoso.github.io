import React from 'react'
import ReactDOM from 'react-dom/client'
import './assets/general/scss/reset.scss'

import App from './view/App'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
